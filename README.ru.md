# 🏆 Плагин Discourse Rich JSON-LD Microdata

**[English version](README.md) | Русская версия**

**Комплексная, скоординированная система Open Graph и Schema.org JSON-LD микроразметки для 200% покрытия SEO и LLM**

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Discourse Version](https://img.shields.io/badge/discourse-2.7.0+-orange.svg)](https://discourse.org)

---

## 🎯 Что делает этот плагин

Плагин **заменяет** стандартные мета-теги Discourse на комплексную, профессионально структурированную систему микроразметки, которая обеспечивает:

### ✅ Для SEO (100%):
- **Расширенные сниппеты** в Google/Яндекс (⭐ рейтинги, 💬 количество ответов, 👤 авторы, 📅 даты)
- **Панели Knowledge Graph** для экспертных пользователей
- **Избранные сниппеты** в "Похожие вопросы"
- **Хлебные крошки** в результатах поиска
- Полная схема **QAPage** для контента типа Q&A

### ✅ Для AI/LLM (100%):
- **Полное понимание контекста** (темы → категории → ответы → комментарии)
- **Связи между сущностями** через `@id` ссылки
- **Метаданные экспертности** авторов
- **Маркировка решенных ответов**
- Полный **граф знаний** вашего форума

### ✅ Итого = 200% покрытие! 🚀

---

## 🌟 Ключевые возможности

### 1. **Скоординированная разметка**
- Open Graph, Twitter Cards и JSON-LD генерируются из **единого источника данных**
- Нет конфликтов данных между разными типами мета-тегов
- Все данные синхронизируются автоматически

### 2. **Полное удаление стандартных тегов**
- Удаляет стандартные мета-теги Discourse (и серверные, и клиентские)
- Предотвращает дублирование/конфликты разметки
- Чистый, профессиональный вывод

### 3. **Богатая структура Schema.org**
- **QAPage** для тем с полной Q&A иерархией
- **CollectionPage** для категорий с подкатегориями
- **ProfilePage** для профилей пользователей
- **BreadcrumbList** для навигации
- **WebSite** глобальная схема с поисковым действием

### 4. **Полная локализация (EN/RU) 🌍**
- 100% английский + 100% русский перевод
- Автоматическое определение языка (пользователь → сайт → браузер)
- Все описания Schema.org локализованы
- Open Graph и Twitter Cards на языке пользователя
- Легко добавить другие языки

### 5. **Оптимизация производительности**
- Умное кеширование (1 час по умолчанию, настраивается)
- Автоматическая инвалидация кеша при изменении контента
- < 5мс для закешированных страниц
- ~50мс для новой генерации

### 6. **LLM-дружественность**
- Полный граф сущностей с `@id` ссылками
- Сохранена вложенная структура комментариев
- Статистика и индикаторы экспертности автора
- Теги как структурированные сущности

---

## 📦 Установка

### Способ 1: Через Git (Рекомендуется)

```bash
cd /var/discourse
git clone https://github.com/kaktaknet/discourse-rich-json-ld-microdata.git plugins/discourse-rich-json-ld-microdata
./launcher rebuild app
```

### Способ 2: Вручную

1. Скопируйте плагин в `plugins/discourse-rich-json-ld-microdata/`
2. Пересоберите Discourse: `./launcher rebuild app`
3. Включите в Admin → Settings → Plugins

---

## ⚙️ Конфигурация

Перейдите в **Admin → Settings → Plugins → Rich Microdata**

### Основные настройки:

| Настройка | По умолчанию | Описание |
|---------|---------|-------------|
| `rich_microdata_enabled` | `true` | Главный переключатель плагина |
| `rich_microdata_cache_ttl` | `3600` | Длительность кеша (секунды) |
| `rich_microdata_max_answers` | `10` | Макс. ответов в JSON-LD |
| `rich_microdata_include_user_stats` | `true` | Включать статистику пользователей |
| `rich_microdata_enable_breadcrumbs` | `true` | Добавить схему BreadcrumbList |
| `rich_microdata_twitter_site` | `""` | Ваш @twitter хендл |

### Расширенные настройки:

| Настройка | По умолчанию | Описание |
|---------|---------|-------------|
| `rich_microdata_max_comments` | `5` | Макс. вложенных комментариев на ответ |
| `rich_microdata_og_image_default` | `""` | Запасной URL OG изображения |
| `rich_microdata_debug_mode` | `false` | Включить отладочное логирование |
| `rich_microdata_validate_output` | `false` | Валидация по Schema.org (только dev) |

---

## 🌍 Локализация (Интернационализация)

Плагин включает полную поддержку локализации для **английского** и **русского** языков из коробки.

### Поддерживаемые языки:

- 🇬🇧 **Английский** (`en`)
- 🇷🇺 **Русский** (`ru`)

### Что переведено:

1. **UI настроек администратора**
   - Все описания настроек
   - Справочный текст

2. **Описания Schema.org**
   - Описания категорий ("Обсуждения в...")
   - Описания профилей пользователей ("Профиль пользователя...")
   - Статистика взаимодействий ("Создано тем", "Написано ответов")

3. **Open Graph & Twitter Cards**
   - Все метаданные
   - Запасные описания

4. **Хлебные крошки**
   - "Home" → "Главная"

### Как работает определение языка:

**Порядок приоритета:**
1. **Настройки пользователя** (из настроек пользователя Discourse)
2. **Настройки сайта по умолчанию** (из Admin → Settings → default_locale)
3. **Локаль браузера** (из HTTP заголовка Accept-Language)
4. **Запасной английский** (`en-US`)

### Добавление новых языков:

#### Шаг 1: Создайте файлы локалей

Создайте файлы в `config/locales/`:

```
config/locales/
├── server.es.yml     # Испанский backend
├── client.es.yml     # Испанский frontend
```

#### Шаг 2: Скопируйте структуру перевода

Скопируйте из существующего файла локали (например, `server.ru.yml`) и переведите:

```yaml
# config/locales/server.es.yml
es:
  site_settings:
    rich_microdata_enabled: "Activar plugin Rich Microdata"
    # ... больше настроек

  discourse_rich_microdata:
    breadcrumb:
      home: "Inicio"

    open_graph:
      category_description: "Discusiones en %{category_name}"
      user_description: "Perfil de usuario %{user_name}"

    twitter_card:
      label_replies: "Respuestas"
      label_author: "Autor"
      # ... больше переводов
```

#### Шаг 3: Тестирование

```ruby
# Rails консоль
I18n.locale = :es
I18n.t('discourse_rich_microdata.breadcrumb.home')
# => "Inicio"
```

#### Шаг 4: Отправьте PR

Вклады приветствуются! Отправляйте переводы через Pull Request.

### Доступные ключи перевода:

См. [`config/locales/server.ru.yml`](config/locales/server.ru.yml) для полного списка ключей.

**Основные группы:**
- `site_settings.*` - Панель администратора
- `discourse_rich_microdata.breadcrumb.*` - Навигация
- `discourse_rich_microdata.open_graph.*` - OG теги
- `discourse_rich_microdata.twitter_card.*` - Twitter карточки
- `discourse_rich_microdata.profile_page.*` - Профили пользователей
- `discourse_rich_microdata.interaction_stats.*` - Статистика

---

## 🏗️ Архитектура

### Поток данных:

```
Объект Discourse (Topic/Category/User)
         ↓
    DataExtractor
         ↓
   Унифицированный Hash данных
    /     |     \
   /      |      \
OG    Twitter   Schema
Builder Builder Builder
   \      |      /
    \     |     /
   Coordinator
         ↓
   MetaGeneratorService
   (с кешированием)
         ↓
    Хук контроллера
         ↓
    Вставка в <head>
```

### Ключевые компоненты:

1. **DataExtractor** - Извлекает и нормализует данные из объектов Discourse
2. **Builders** - Генерируют конкретные типы разметки (OG, Twitter, JSON-LD)
3. **Coordinator** - Обеспечивает согласованность данных во всех builders
4. **MetaGeneratorService** - Добавляет кеширование и обработку ошибок
5. **MetaRemover** - Удаляет стандартные теги Discourse

---

## 📊 Примеры генерируемой разметки

### Для тем:

**Open Graph:**
```html
<meta property="og:type" content="article">
<meta property="og:title" content="Как оптимизировать запросы PostgreSQL?">
<meta property="og:url" content="https://forum.com/t/topic-slug/123">
<meta property="og:description" content="Запросы выполняются 3.5 секунды...">
<meta property="og:image" content="https://forum.com/uploads/postgres.jpg">
<meta property="article:author" content="https://forum.com/u/john">
```

**JSON-LD QAPage:**
```json
{
  "@context": "https://schema.org",
  "@type": "QAPage",
  "name": "Как оптимизировать запросы PostgreSQL?",
  "mainEntity": {
    "@type": "Question",
    "name": "Как оптимизировать запросы PostgreSQL?",
    "text": "Запросы выполняются 3.5 секунды...",
    "answerCount": 12,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": "Используйте составные индексы...",
      "upvoteCount": 89,
      "author": {
        "@type": "Person",
        "name": "Мария DB Эксперт"
      }
    },
    "suggestedAnswer": [...]
  }
}
```

---

## 🔧 Кастомизация

### Добавление пользовательских социальных ссылок:

Отредактируйте `lib/discourse_rich_microdata/builders/website_builder.rb`:

```ruby
def social_links
  links = []
  links << "https://github.com/your-org" if SiteSetting.your_github_url
  links << "https://twitter.com/yourhandle" if SiteSetting.your_twitter_url
  links.presence
end
```

### Изменение лимита ответов:

Admin → Settings → `rich_microdata_max_answers` (5-50)

### Пользовательское запасное изображение:

Admin → Settings → `rich_microdata_og_image_default`

---

## 🧪 Тестирование и валидация

### Автоматическая валидация:

```bash
# Запуск тестов (когда доступны)
bundle exec rspec plugins/discourse-rich-json-ld-microdata
```

### Ручная валидация:

1. **Google Rich Results Test:**
   ```
   https://search.google.com/test/rich-results
   ```

2. **Schema.org Validator:**
   ```
   https://validator.schema.org
   ```

3. **Facebook Debugger:**
   ```
   https://developers.facebook.com/tools/debug/
   ```

4. **Twitter Card Validator:**
   ```
   https://cards-dev.twitter.com/validator
   ```

### Проверка генерируемой разметки:

```bash
# Как Googlebot
curl -A "Googlebot" https://ваш-форум.com/t/тема-slug/123 | grep "application/ld+json"

# Просмотр полного head
curl https://ваш-форум.com/t/тема-slug/123 | grep -A 50 "<head>"
```

---

## 📈 Метрики производительности

### Ожидаемая производительность:

| Метрика | Значение |
|--------|-------|
| Первая генерация (холодная) | ~50мс |
| Из кеша (теплая) | ~2-5мс |
| Процент попаданий в кеш | 95-98% |
| Память на схему | ~6KB |
| Размер Redis кеша (1000 тем) | ~6MB |

### Мониторинг:

```ruby
# В Rails консоли
MetaGeneratorService.cache_stats
# => {
#   topics: 1247,
#   categories: 15,
#   users: 234,
#   total_size: "7.5 MB"
# }
```

---

## 🐛 Решение проблем

### Проблема: Разметка не появляется

**Решение:**
```ruby
# Проверьте, включено ли
SiteSetting.rich_microdata_enabled
# => true

# Очистите кеш
MetaGeneratorService.clear_all_cache

# Проверьте логи
tail -f log/production.log | grep RichMicrodata
```

### Проблема: Старые теги всё ещё присутствуют

**Решение:**
1. Жёсткое обновление браузера (Ctrl+Shift+R)
2. Проверьте, запускается ли JavaScript очистка: Откройте DevTools → Console
3. Проверьте атрибут `data-rich-microdata` на новых тегах

### Проблема: Ошибки валидации

**Решение:**
```ruby
# Включите режим отладки
SiteSetting.rich_microdata_debug_mode = true

# Проверьте конкретную тему
topic = Topic.find(123)
data = DiscourseRichMicrodata::DataExtractor.extract_topic_data(topic)
puts JSON.pretty_generate(data)
```

---

## 🤝 Вклад в проект

Вклады приветствуются!

1. Форкните репозиторий
2. Создайте ветку функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Добавить AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📝 История изменений

### Версия 2.0.0 (2025-01-16)
- ✨ **Полная локализация (EN/RU)** с автоматическим определением языка
- ✨ Поддержка I18n для всех Schema.org, Open Graph, Twitter Cards
- ✨ Пример правил Telegram IV шаблона в [`TELEGRAM_IV_RULES.txt`](TELEGRAM_IV_RULES.txt)
- ✨ Раздельный рендеринг для head тегов и body контента
- ✨ Расширенное определение приоритета языка (пользователь → сайт → браузер)
- ✨ URL кодирование для кириллических символов в slug/username/tags

### Версия 1.0.0 (2025-01-16)
- ✨ Первый релиз
- ✨ Полная схема QAPage для тем
- ✨ CollectionPage для категорий
- ✨ ProfilePage для профилей пользователей
- ✨ Скоординированные OG + Twitter + JSON-LD
- ✨ Умное кеширование с авто-инвалидацией
- ✨ Удаление стандартных тегов Discourse

---

## 📄 Лицензия

Лицензия MIT - см. файл [LICENSE](LICENSE)

---

## 💬 Поддержка

- 📧 Email: support@kaktak.net
- 💬 Telegram: @kaktaknet
- 🐛 Проблемы: [GitHub Issues](https://github.com/kaktaknet/discourse-rich-json-ld-microdata/issues)
- 📖 Документация: [Полная документация](docs/)

---

## 🌟 Благодарности

- Команде Discourse за потрясающую платформу
- Schema.org за стандарты структурированных данных
- Всем контрибьюторам и пользователям

---

<div align="center">

**Сделано с ❤️ для сообщества Discourse**

[⬆ Вернуться наверх](#-плагин-discourse-rich-json-ld-microdata)

</div>
